name: cicd

on:
  - push

permissions:
  contents: read
  packages: write

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Install Taskfile
        uses: ./.github/actions/taskfile

      - name: Linters
        run: task lint

  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Install Taskfile
        uses: ./.github/actions/taskfile

      - name: Run Tests
        run: task tests

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v5

      - name: Set Up Docker Sdk
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine Docker tags (main)
        if: github.ref == 'refs/heads/main'
        run: |
          IMAGE_NAME="ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/crypto-positions-manager"
          IMAGE_TAG="main-${GITHUB_SHA::7}"
          LATEST_TAG="latest"
          
          echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_ENV
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "LATEST_TAG=$LATEST_TAG" >> $GITHUB_ENV

      - name: Determine Docker tags (other branches)
        if: github.ref != 'refs/heads/main'
        run: |
          BRANCH_NAME=${GITHUB_REF#refs/heads/}
          SAFE_BRANCH=${BRANCH_NAME//\//-}
          
          IMAGE_NAME="ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/crypto-positions-manager"
          IMAGE_TAG="${SAFE_BRANCH}-${GITHUB_SHA::7}"
          LATEST_TAG="${SAFE_BRANCH}-latest"
          
          echo "IMAGE_NAME=$IMAGE_NAME" >> $GITHUB_ENV
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "LATEST_TAG=$LATEST_TAG" >> $GITHUB_ENV

      - name: Build and Push Docker images
        run: |
          docker buildx build \
            -t $IMAGE_NAME:$IMAGE_TAG \
            -t $IMAGE_NAME:$LATEST_TAG \
            -f build/Dockerfile \
            --push .
